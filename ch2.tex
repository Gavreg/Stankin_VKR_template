\section{Структура и оформление работы}

В главе будут приведены выдержки из положения в ВКР\cite{stankin:vkr} и даны комментарии о реализации этих пунктов в \LaTeX{}.



\subsection{ПАРАГРАФ 1}
О размере и объёме ВКР документа в положении написано следующее:

\noindent\begin{tabularx}{\linewidth}{||X@{}}
<<6.1. ВКР должна быть представлена в форме рукописи в печатном виде на листах белой
бумаги формата А4 (210х297 мм), на одной стороне листа.\\
6.2. Общий объем ВКР магистранта должен быть не менее 60 страниц, специалиста -50
страниц, бакалавра - 40 страниц (без приложений).\\
6.3. Каждая страница должна иметь одинаковые поля: размер левого поля - 30 мм,
правого - 15 мм, верхнего и нижнего - по 20 мм, без рамки.>>	
\end{tabularx}

Обеспечивается это требование пакетом geometry, который Добавлен в самом начале преамбулы. Ознакомится с структурой страниц можно если включить режим отладки, раскомментировав \verb|\Debugtrue| в начале \verb|main.tex|.

В качестве требований к шрифу в положении указано так:
\noindent\begin{tabularx}{\linewidth}{||X@{}}
	<<6.4. Набор текста осуществляется по следующим требованиям: шрифт Times New
	Roman, кегль 14, межстрочный интервал - 1,5. Текст форматируется по ширине страницы без
	применения автоматического переноса слов, первая строка абзаца с отступом 1,25 мм.>>	
\end{tabularx}
В силу возраста у классических \TeX{} и \LaTeX{} большая проблема с современными TypeScript шрифтами. На основании этого будет использовать \XeLaTeX{} --- солее современную версию компилятора разметки. Переключить систему сборки в Tex Studio можно на настройках программы в разделе <<Компиляция>>.




\subsubsection{Подпараграф}

\paragraph{ПодПодПод}

\subsubsection{Подпараграф}


\subsection{ПАРАГРАФ 2}

\subsubsection{Подпараграф}
\subsubsection{Подпараграф}